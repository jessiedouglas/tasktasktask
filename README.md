A simple TODO list web app for Apps Script with Google Sheets.

This application requires a sheet with the following named ranges that each contain one column: "id", "task", "status", and "last_status_update".
